mixin forgotPasswordForm()
    form(action="/account/forgot" method="post")
        .form-group
            label(for="email") Email
            input(name="email" class="form-control" type="email" placeholder="you@example.com")
        input(type="submit" class="btn btn-secondary btn-block mt-3" value="Send a Reset")
