doctype html
html
    head
        title= `${title ? title : h.siteName}`
        meta(name="viewport" content="width=device-width, initial-scale=1")
    body
        .content
            block content
